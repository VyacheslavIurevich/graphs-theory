[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "graphs-theory-task1"
version = "0.1.0"
description = "Task 1 benchmark environment for spla-bench"
readme = "spla-bench/README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
]

[tool.setuptools]
packages = []
